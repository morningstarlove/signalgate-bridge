name: SIGNAL GATE daily bridge

on:
  schedule:
    - cron: "18 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Fetch Upbit data
        id: fetch
        continue-on-error: true
        run: python fetch.py

      - name: Commit status.json
        if: always()
        run: |
          git config user.name  "signalgate-bot"
          git config user.email "signalgate-bot@users.noreply.github.com"
          git add -A status.json history.jsonl || true
          if git diff --staged --quiet; then
            echo "no change"
          else
            git commit -m "status $(date -u +%Y-%m-%d)"
            git push
          fi
